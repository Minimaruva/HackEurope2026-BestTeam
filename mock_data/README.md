# Mock Data (Mandatory)

All demo flows must use local, deterministic data from this directory.

## Subfolders
- `invoices/`: pre-extracted JSON dictionaries (no OCR dependency)
- `market_benchmarks/`: synthetic benchmark feeds (no external API dependency)
- `contracts/`: synthetic MSAs with injected DORA compliance failures

> Do not rely on network calls during live presentation.
